<view class="text-center h100 op60 backdrop-blur flex justify-around">
  <view
    wx:for="{{tabs}}"
    class="flex-1 fc border-base {{item.name === curTab && 'bg-neutral-200_50'}}"
    data-value="{{item.name}}"
    @tap="tap">
    {{item.name}}
  </view>
</view>
